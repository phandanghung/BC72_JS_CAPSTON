export class CartItem {
  constructor(id, name, price, img, desc, type, quanity) {
    (this.id = id),
      (this.name = name),
      (this.price = price),
      (this.img = img),
      (this.desc = desc),
      (this.type = type),
      (this.quanity = quanity);
  }
}
